/**
 * Contains the definition of a PostgreSQL database connection class, which is used to read and write layers to a
 * database representation.  Includes a single class (DatabaseConnection)
 *
 */
package database;