/**
 * Contains renderer objects that render the colored patch (GeometryTableIconRenderer) and the remove button
 * (LayerRemoveButtonRenderer) for each layer instance in the table of contents.
 */
package renderers_and_editors;